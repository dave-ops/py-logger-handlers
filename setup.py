from setuptools import setup

setup(
    name='logger-handlers',
    packages=['logger_handlers'],
    description='Allows your logger to send logs to SQL and Email',
    version='1.0.4',
    url='http://pip.code-ops.com',
    author='code-ops',
    author_email='python@code-ops.com',
    keywords=['pip', 'logger', 'sql', 'email'],
    include_package_data=True
)
